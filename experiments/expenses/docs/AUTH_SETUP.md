# Authentication Setup Guide

Your Streamlit app now has password authentication to protect your personal expense data.

## Local Login Credentials

**For testing (current setup):**
- Username: `admin`
- Password: `testpass123`

run_local.sh has the setup for local testing.

Run from repo root:

`experiments/expenses/streamlit/run_local.sh`

## Setting Environment Variables

You need these environment variables, with the value for AUTH_PASSWORD_HASH generated using instructions further down.

```bash
# Set environment variables
export AUTH_USERNAME="admin"
export AUTH_NAME="Admin User"
export AUTH_PASSWORD_HASH="your_hash_here"

### Generate a Password Hash

Use the included `hash_password.py` script:

```bash
.venv/bin/python experiments/expenses/streamlit/hash_password.py
```

This will output a bcrypt hash that you can copy and use as `AUTH_PASSWORD_HASH` (see details below).

### Running the App Locally

this might work...

```
streamlit run experiments/expenses/streamlit/app.py
```

but more reliable (same as shown at top)...

`experiments/expenses/streamlit/run_local.sh`

## Railway Deployment

**IMPORTANT:** Change the password before deploying. Generate a new hash with `experiments/expenses/streamlit/hash_password.py` using a secure password.

Add these environment variables in your Railway project settings:

1. Go to your Railway project
2. Click on "Variables" tab
3. Add these variables:
   - `AUTH_USERNAME`: Your desired username (e.g., "admin")
   - `AUTH_NAME`: Display name (e.g., "Your Name")
   - `AUTH_PASSWORD_HASH`: The bcrypt hash generated by `hash_password.py`
   - `GOOGLE_CREDENTIALS_JSON`: Your Google service account JSON (entire file content)

## Security Notes

- Never commit your password hashes to git
- Use strong, unique passwords for production
- The app uses bcrypt for secure password hashing
- Sessions expire after 30 days (configurable in `experiments/expenses/streamlit/app.py`)
- Always use HTTPS in production (Railway provides this automatically)
