# Authentication credentials
# Generate password hash using: python generate_password.py
AUTH_USERNAME=admin
AUTH_NAME=Admin User
AUTH_PASSWORD_HASH=your_hashed_password_here

# Google Sheets credentials (for Railway deployment)
# Copy the entire contents of your credentials JSON file
GOOGLE_CREDENTIALS_JSON={"type":"service_account",...}
