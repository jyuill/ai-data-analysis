[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["uv sync"]

[start]
cmd = "uv run streamlit run experiments/expenses/streamlit/app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
